const colors = {
  primary: '#0606f9',
  secondary: '#e2dfcf',
  black: '#0d0d0d',
  gray: '#f1f1f1',
  white: '#ffffff',
};

const fonts = {
  monospace: "'Overpass Mono', monospace",
  sans: "'Overpass', sans-serif",
};

const theme = {
  color: { ...colors },
  font: { ...fonts },
};

export default theme;
